CHIP Computer {

    IN reset;

    PARTS:
    ROM32K(address=pcOut, out=romOut);
    Memory(in=outM, load=writeM, address=addressM, out=inM);
    CPU(inM=inM, instruction=romOut, reset=reset,
        outM=outM, writeM=writeM,
        addressM=addressM, pc=pcOut
    );
}
