// CREATED FROM SCRATCH ON: 1.15.26

/** 
 * Multiplexor:
 * if (sel = 0) out = a, else out = b
 */
CHIP Mux {
    IN a, b, sel;
    OUT out;

    PARTS:
    // 1.15.26
    Not(in=sel, out=notSel);
    And(a=a, b=notSel, out=aOut);
    And(a=b, b=sel, out=bOut);
    Or(a=aOut, b=bOut, out= out);
}